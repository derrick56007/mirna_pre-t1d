def visualize_data(file_url, raw_dir, file_name, temp_dir):
    print("Visualizing data")
    
    # Most of our visualizations will be performed using R,
    # we have not currently figured out how to interop R code with python code.
    # Our next goal is to properly configure a docker file so that
    # both R and python dependencies can be solved
    
    print("Done")
    return